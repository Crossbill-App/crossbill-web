# The app and the API share an origin by default: the dev server proxies /api
# to the backend on http://localhost:8000, and production serves both from the
# same FastAPI app. Set this only to point the frontend at a backend somewhere
# else — it makes the API cross-origin, which the reader's iframes cannot use.
# VITE_API_URL=http://localhost:8000

