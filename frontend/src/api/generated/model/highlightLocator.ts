/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * crossbill API
 * OpenAPI spec version: 0.0.0
 */
import type { LocatorSchema } from './locatorSchema.ts';
import type { LocatorUnavailable } from './locatorUnavailable.ts';

/**
 * Where a highlight is in the EPUB, or why that cannot be said.
 *
 * Exactly one of the two fields is set, and a client must handle both: a
 * locator here is derived from the stored xpointer at request time and
 * verified against the highlight's own text, so a book whose EPUB has been
 * replaced answers with reasons rather than with places (ADR-0004 §5).
 */
export interface HighlightLocator {
  /** The verified Readium locator, with hrefs in the same coordinates the manifest publishes. Null when the highlight cannot be placed. */
  locator?: LocatorSchema | null;
  /** Why there is no locator: 'no_ebook' (the book has no EPUB), 'not_placeable' (the highlight was never given a position), 'unresolved' (its position names nothing in this EPUB) or 'text_mismatch' (it resolved, but onto different text -- the shape a replaced edition takes). Null when the locator is present. */
  unavailable?: LocatorUnavailable | null;
}
