/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * crossbill API
 * OpenAPI spec version: 0.0.0
 */
import type { LocatorSchema } from './locatorSchema.ts';
import type { PositionResponse } from './positionResponse.ts';
import type { ResumeSource } from './resumeSource.ts';

/**
 * Where the web reader should open a book, whichever device was there last.
 *
 * Always an object, never null. A book nobody has read and a book whose stored
 * place no longer exists both open at the beginning, but only the second is
 * worth telling the reader about, and a null body could not tell them apart.
 * Read it as: navigate to ``locator`` if there is one; otherwise start at the
 * beginning, and say so if ``unresolved``.
 */
export interface ResumePositionResponse {
  /** Where to open the book, ready to navigate to, or null when there is nowhere to resume to */
  locator?: LocatorSchema | null;
  /** Which reader the position came from -- 'web' for this browser's own stored place, 'koreader' for the end of a session synced from an e-reader. Null when the book has never been read anywhere. */
  source?: ResumeSource | null;
  /** Whether a position exists that could not be placed in the EPUB this server now holds -- typically the file has been replaced. The book opens at the beginning and the reader is told their place was lost. */
  unresolved?: boolean;
  /** The position in the format both readers agree on, present even when no locator could be derived from it */
  xpoint?: string | null;
  /** The position in document order, or null when it could not be placed */
  position?: PositionResponse | null;
  /** When the reader was at this position, by their own device's clock */
  recorded_at?: string | null;
}
